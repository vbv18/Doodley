import { Router } from "express";


const userRouter: Router = Router();

// // GET /api/v1/users/me
// userRouter.get("/me");

// // PATCH /users/me
// userRouter.patch("/me");

// // GET /users/:id
// userRouter.get("/:id");


export default userRouter;